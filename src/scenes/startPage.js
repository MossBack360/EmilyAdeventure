class StartPage {
    constructor(playerInfo) {
        this.playerInfo = playerInfo;
        this.scene = null;
    }

    init() {
        // Initialize the start page scene
    }

    show() {
        // Show the start page
    }

    hide() {
        // Hide the start page
    }
}